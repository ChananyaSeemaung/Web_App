# CONTEXT.md — เว็บพอร์ตโฟลิโอ

เอกสารบันทึกบริบทและการตัดสินใจของโปรเจ็ค อัปเดตล่าสุด: 2026-08-04
สถานะ: **ยังอยู่ในขั้นซักถาม (grilling) — ยังไม่เริ่มเขียนโค้ด**

---

## 1. เป้าหมายและผู้ใช้

| หัวข้อ | สรุป |
|---|---|
| เป้าหมาย | สมัครงาน developer ในไทย |
| ผู้อ่านหลัก | HR (สแกนเร็ว ~30 วินาที) + tech lead (ขอดูโค้ดจริง) |
| Primary task ของผู้อ่าน | ตัดสินใจได้เร็วว่า "คนนี้ทำเป็นจริง ควรเรียกสัมภาษณ์" |
| ตำแหน่งที่เล็ง | Frontend / Full-stack ที่ออกแบบได้ |

## 2. เนื้อหาที่จะใส่

เนื้อหา 3 ชนิด ที่ต้องการการนำเสนอต่างกัน:

1. **โปรเจ็คโค้ด** — พระเอก
   - `Web/` ร้านค้าออนไลน์ PHP + MySQL (ผลงานเก่า)
   - ตัวเว็บพอร์ตโฟลิโอนี้เอง (React + Node + Postgres) นับเป็นผลงานชิ้นหนึ่ง
2. **งานกราฟิก / ออกแบบ** — หลักฐานสนับสนุนว่า "ออกแบบเองได้" มี section ของตัวเอง แต่ไม่แย่งหน้าแรก
3. **เกียรติบัตร / รางวัล** — หลักฐานประกอบ ไม่ใช่พระเอก ควรอยู่หน้า About หรือหน้ารวมเดียว

## 3. ภาษา

- **ไทยเป็นหลัก + ศัพท์เทคนิคเป็นอังกฤษ**
- **ไม่ทำ i18n** → Prisma schema ไม่ต้องมี field ภาษาคู่, ไม่มีปุ่มสลับภาษา
- ผลต่อดีไซน์: ต้องเลือกฟอนต์ไทยที่มีน้ำหนักครบและอ่านง่ายในขนาดเล็ก (เช่น IBM Plex Sans Thai, Noto Sans Thai, Anuphan) — line-height ต้องเผื่อสระบน/ล่างมากกว่าฟอนต์ละติน

## 4. ขอบเขตงาน: มี 2 ผลิตภัณฑ์ ไม่ใช่ 1

> ⚠️ **แก้ความเข้าใจผิด (2026-08-04):** ตอนแรกเข้าใจว่า Postgres + Prisma + Node เป็น backend ของ *ตัวพอร์ตโฟลิโอ* — **ผิด**
> ผู้ใช้ยืนยันว่า stack นั้นคือของ **ร้านค้าเวอร์ชันใหม่** ที่จะเอา `Web/` มาเขียนใหม่

### 4.1 ผลิตภัณฑ์ A — เว็บพอร์ตโฟลิโอ
- หน้าที่: เก็บและนำเสนอผลงาน เพื่อสมัครงาน dev ในไทย
- Stack: **React + TypeScript + Tailwind CSS (Vite)**
- โค้ดเดิมที่มีอยู่: `client/` (ยังเป็น Vite template)
- ยังไม่ตัดสินใจ: ต้องมี backend/DB เองไหม หรือเก็บข้อมูลผลงานเป็นไฟล์

### 4.2 ผลิตภัณฑ์ B — ร้านค้าออนไลน์เวอร์ชันใหม่ (rewrite ของ `Web/`)
- หน้าที่: เอาโปรเจ็คเก่า PHP + MySQL + Bootstrap มาพัฒนาใหม่ด้วยเทคโนโลยีปัจจุบัน
- Stack: **React + TypeScript + Tailwind CSS** / **Node.js + TypeScript** / **PostgreSQL + Prisma ORM**
- บทบาทในพอร์ต: **ผลงานเอก** — เป็นชิ้นที่ tech lead จะเปิดดูโค้ดจริง และเป็นหลักฐาน full-stack
- จุดแข็งที่มีอยู่แล้ว: โปรเจ็คเก่ามีฟีเจอร์ครบวงจรจริง (ตะกร้า → ชำระเงินโอน+สลิป → EMS → admin) และมี asset จริงให้ใช้ต่อ ไม่ต้องปั้นข้อมูลปลอม

**ทำไมการแยกนี้สำคัญ:** Postgres/Prisma อยู่ในที่ที่สมเหตุสมผลแล้ว (ร้านค้ามีธุรกรรมจริง ต้องการ relation และ transaction) ส่วนพอร์ตโฟลิโออาจไม่ต้องมี DB เลยก็ได้

---

## 5. สถานะ Repository ตามจริง (ตรวจเมื่อ 2026-08-04)

### `client/` — Vite template ที่ยังไม่แตะ
- React 19.2, TypeScript 6.0, Vite 8.2, `oxlint` เป็น linter
- **ยังไม่มี Tailwind, ไม่มี router, ไม่มี data fetching**
- `src/App.tsx` ยังเป็นหน้า "Get started" ของ Vite ทั้งหน้า
- `src/index.css` มี `--accent: #aa3bff` (สีม่วง) = สีของ template ไม่ใช่แบรนด์ผู้ใช้
- **สรุป: ถือเป็น greenfield ไม่มีหลักฐานแบรนด์เดิมให้สืบทอด**

### `Web/` — ผลงานเก่า: ร้านค้าออนไลน์ PHP + MySQL
- ~80 ไฟล์ PHP, Bootstrap 5.1.3, แยกฝั่ง admin (`ad_*.php`) กับฝั่งลูกค้าชัดเจน
- ฟีเจอร์จริงที่จับต้องได้:
  - สินค้าและหมวดหมู่, ตะกร้าสินค้า
  - ใบเสนอราคา (`quotation.php`)
  - ชำระเงินโอนธนาคาร + อัปโหลดสลิป (`insert_payment.php`, `bill/`)
  - ติดตามพัสดุ EMS (`ad_ems*.php`)
  - จัดการสมาชิก, ยกเลิก/คืนออร์เดอร์ (`cancel_order.php`, `re_order*.php`)
- `pro.sql` มี 8 ตารางใช้งานจริง: `product`, `product_type`, `order_tbl`, `order_detail`, `payment`, `bank`, `member_tbl`, `users`
- ⚠️ **`pro.sql` มีตาราง Django ปนอยู่** (`accounts_customuser`, `auth_permission`, `django_content_type`, `auth_group`, `auth_user`) — น่าจะเป็นเศษจาก dump คนละโปรเจ็ค ต้องตัดออกถ้าจะอ้างอิง schema นี้
- **Asset จริงที่ใช้เป็นหลักฐานได้:** `img/` 43 รูปสินค้า, `ad_image/` 55, `us_image/` 55, `b_img/` 14, `bill/` 12 สลิป, `video/` 2 คลิป

### ยังไม่มีในโปรเจ็ค
- ไม่มีโฟลเดอร์ backend / server
- ไม่มี Prisma schema, ไม่มี Postgres
- ไม่มี monorepo config (workspace/turbo/pnpm)
- ไม่มี `DESIGN.md` / `PRODUCT.md`

---

## 6. สกิลที่ติดตั้งและกฎการทำงาน

ติดตั้ง global ที่ `~/.agents/skills/` symlink เข้า `~/.claude/skills/`:

| สกิล | แหล่ง | หน้าที่ |
|---|---|---|
| `gridgeist` | `ohmiler/gridgeist` | เจ้าของทิศทางภาพและ product direction |
| `impeccable` | `pbakaus/impeccable` | คราฟต์ UI, polish, critique, audit, animate, adapt |
| `grill-me` | `mattpocock/skills@grill-me` | ซักถามให้ชัดก่อนลงมือ |
| `grilling` | `mattpocock/skills@grilling` | สกิลที่ `grill-me` เรียกใช้ (ต้องมีคู่กัน) |

**กฎที่ผู้ใช้ตั้งไว้:**
- ใช้ทั้ง 3 สกิลทุกครั้งในงาน UI
- **ตรวจ repository และหน้าเว็บที่ render จริงก่อนเริ่มทำงานเสมอ**
- ถ้าทิศทางภาพยังไม่ยืนยัน → เสนอ 2–3 ทิศทางพร้อม trade-off และคำแนะนำ แล้ว **หยุด ไม่แก้โค้ด** จนผู้ใช้เลือก
- `gridgeist` เป็นเจ้าของทิศทางภาพเพียงคนเดียว (ไม่ผสม thesis กับ `impeccable`) — `impeccable` ใช้ช่วยด้าน implementation craft และ verification เท่านั้น

---

## 7. คำถามที่ยังไม่ได้คำตอบ

### สรุปสถาปัตยกรรมที่ตัดสินใจแล้ว

```
repo: portfolio            (ทำก่อน)
  React + TS + Tailwind + Vite
  ข้อมูลผลงาน = ไฟล์ TS/MDX ใน repo, ไม่มี backend
  deploy: Vercel (static)

repo: shop-v2              (ทำทีหลัง)
  client:  React + TS + Tailwind      → deploy Vercel
  server:  Node.js + TS + Prisma      → deploy Render
  database: PostgreSQL                → managed (Render / Neon / Supabase)

repo: shop-legacy-archive  (ของเดิม `Web/` เก็บไว้อ้างอิง)
```

### คำถามที่ยังไม่ได้คำตอบ

**ของพอร์ตโฟลิโอ (A) — ต้องตอบก่อนเริ่ม**

| # | หัวข้อ | สถานะ |
|---|---|---|
| 9 | **ทิศทางภาพของ A** | *provisional* — กำลังเสนอ 2–3 ทิศทาง รอผู้ใช้เลือก |
| 10 | ข้อมูลตัวจริง (ชื่อ, ตำแหน่ง, ช่องทางติดต่อ, GitHub, รูป) | ยังไม่มี ต้องขอจากผู้ใช้ |
| 11 | ฟอร์มติดต่อ หรือใช้ mailto/LINE/อีเมลตรง | ยังไม่ถาม (พอร์ตไม่มี backend → ถ้าอยากมีฟอร์มต้องใช้บริการนอก) |

**ของร้านค้าใหม่ (B) — เลื่อนไปตอนเริ่ม B**

| # | หัวข้อ | ทำไมสำคัญ |
|---|---|---|
| B1 | ขอบเขต feature | โปรเจ็คเก่ามี ~12 flow — ทำครบ หรือเลือกเฉพาะที่โชว์ฝีมือได้ |
| B2 | ย้ายข้อมูลเก่าไหม | `pro.sql` มีข้อมูลจริง → seed จากของเดิม หรือเริ่มใหม่ |
| B3 | Auth | เดิมเก็บ password ใน `member_tbl` แบบไม่ hash → ต้องออกแบบใหม่ทั้งหมด |
| B4 | Storage รูปภาพ | มีรูปจริง ~180 ไฟล์ Postgres ไม่ควรเก็บไฟล์ → object storage |
| B5 | ทิศทางภาพของ B | ร้านค้าเป็น **Operate mode** ต่างจากพอร์ตที่เป็น **Experience mode** |

---

## 8. บันทึกการตัดสินใจ

| วันที่ | การตัดสินใจ |
|---|---|
| 2026-08-04 | เป้าหมาย = สมัครงาน dev ในไทย |
| 2026-08-04 | เนื้อหา = โปรเจ็คโค้ด + งานกราฟิก + เกียรติบัตร/รางวัล |
| 2026-08-04 | ตำแหน่ง = Frontend/Full-stack ที่ดีไซน์ได้ (โค้ดพระเอก กราฟิกสนับสนุน) |
| 2026-08-04 | ภาษา = ไทยหลัก + ศัพท์เทคอังกฤษ, ไม่ทำ i18n |
| 2026-08-04 | ผู้ใช้สั่ง "อย่าพึ่งลงโค้ด" — ยืนยันว่ายังอยู่ขั้นวางแผน |
| 2026-08-04 | **แก้ขอบเขต:** Postgres + Prisma + Node เป็น stack ของ *ร้านค้าเวอร์ชันใหม่* (rewrite `Web/`) ไม่ใช่ของตัวพอร์ตโฟลิโอ → งานนี้มี 2 ผลิตภัณฑ์ |
| 2026-08-04 | ลำดับงาน = ทำพอร์ตโฟลิโอ (A) ให้เสร็จก่อน แล้วค่อยทำร้านค้าใหม่ (B) |
| 2026-08-04 | พอร์ตโฟลิโอเก็บข้อมูลผลงานเป็นไฟล์ใน repo ไม่มี backend ไม่มี DB |
| 2026-08-04 | แยก repo ต่อผลิตภัณฑ์ (ไม่ทำ monorepo) |
| 2026-08-04 | Deploy: frontend → Vercel, backend ของ B → Render |
